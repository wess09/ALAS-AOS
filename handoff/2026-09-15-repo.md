# Handoff 2026-09-15 · 建仓完成 + 阶段一 M1 开工点

> 新建对话或上下文压缩后：先读本文与 `docs/roadmap-v3.md`，再从「下一步」继续。
> 历史 handoff：`2026-09-15.md`（路线规划收官）、`-spike-a.md`、`-spike-c.md`。

## 当前状态

- **公开仓已建：https://github.com/Shinarin/AzurPilot**（默认分支 `main`，首提交 `f89a43f` = 路线图 v3 + Spike A/C/D 全量成果 + 账册体系，194 文件）。
- 构建路线拍板：**GHA ARM64 runner（ubuntu-24.04-arm）构建 rootfs**，零本地负担；WSL 方案作废不再提。
- 阶段〇 Spike：A ✅ / B′ ✅ / C ✅ / D ✅ / **E ✅（22:44 收口）**：报告 `spike/e-adb-virtual-display/REPORT.md`——推翻 m0 §41（`screencap` 吃 SF physical id、`input` 吃 logical id 均可达 VD），但 VD 保活死穴 + 手势劫持事件 → 维持桥为主、登记诊断/备用。**手势劫持事件**（scrcpy VD 的 SYSTEM_DECORATIONS flag 致主屏手势失效）已处置恢复、设备已清场；硬约束落 `AGENTS.md` 虚拟屏实验纪律 / `debug.md` 4 条坑 / roadmap 阶段二 VD flag 硬约束。/ F 并入阶段一 M1。
- 设备上两条幻影缓解命令保持开启（生产态，Spike C 结论）。

## 下一步（阶段一 M1：rootfs v0.1 + Spike F 精度门禁）

1. m0 资产 curated 进仓：`rootfs/patches/` ← `m0-archive/termux/patches/`、`rootfs/seeds/deploy.yaml`（按 Spike D 七键改定）、`rootfs/models/ocr/` ← `m0-archive/spike/m0/resource/base/model/ocr/`（PP-OCR 三件套 det 9.4MB / rec 20.2MB / keys 75KB）。
2. GHA workflow：Ubuntu base ARM64 + Python3 + opencv-headless + onnxruntime + AzurPilot（Gitee 镜像**钉 commit**，先核实 fullcn 镜像配置 `config_redirect()` 是否重写 gitee/cn 别名到 `git.lyoko.io`）+ 补丁 + 模型 + wrapper 骨架 → `rootfs.tar.xz`（目标 ~250MB）+ **BUILD_MANIFEST**（决策 #10 硬要求：AzurPilot commit / 补丁版本 / 模型版本 / rootfs 版本）。
3. Spike F 双通道验收：GHA runner chroot 跑 PP-OCR 精度（油数 100 次 ≥98%，m0 DoD 口径）作 CI 门禁；再推真机经 proot 复验（复用 `spike/a-proot-exec` harness 与 `run-device-ladder.sh`；坑：`proot -b /dev:/dev`、rootfs 全量 +x）。
4. rpc.py shim 改写：TCP 桥接 AzurPilot → in-proc onnxruntime 加载 det/rec（ModelProxy 签名保持；防 2D 单通道堆叠 3ch、BGR↔RGB 两条 m0 硬坑）。
5. Spike E 通知到后：B′/E 结论汇报用户，视情在 roadmap v3 附录记"adb 控制面可行性"（只影响未来切换，不影响主线）。

## 关键决策与注意

- **push/发版红线不变**：本次建仓 push 是用户逐次授权的；之后每次 push 仍需用户明确指令。devlog/handoff 等账册改动默认只落本地。
- 公开仓内容已含设备序列号 `AVAY025422002864` 与 LAN IP（AGENTS.md/账册原文）——低敏，已向用户报备。
- GHA 免费 ARM64 runner 仅限**公共仓**（这正是主仓先行公开的原因）。
- gh CLI 已登录 `Shinarin`；git 身份 Elysia \<da2701076760@gmail.com\>。
- AzurPilot fullcn = 官方 master 同架构 + 国内镜像 deploy 配置；rootfs 取"官方 master 钉 commit + fullcn 同款镜像配置"。
- 真机调试闭环：设备 `AVAY025422002864`，adb shell 前必须 `export MSYS_NO_PATHCONV=1`；Windows 侧路径用 `cygpath -m`。
