# Handoff 2026-09-18 · v0.1.1 发版收官

> 上一篇：`2026-09-18-boot-visibility.md`（静默期治理 + CDN 热更新双通道）。本篇记 v0.1.1 发版全案与发布后状态。

## 发版事实

- **产物**：`更早的旧名-v0.1.1-android-arm64.apk`（311MB），GitHub Release `v0.1.1` @ [Shinarin/AzurPilot](https://github.com/Shinarin/AzurPilot/releases)。tag `v0.1.1` = commit `976da2e`，versionName `0.1.1`、versionCode 56。
- **内容**（v0.1.0 之后共 10 提交）：CDN 热更新双通道（开机链 4m04s→5s）、启动静默期治理（session.log `[host]` 时间线 + 挂机页阶段明细）、azur_lane 字体 OCR 纯 numpy 上机（与桌面逐位一致）、D3 崩溃双根因修复、保屏唤醒、env_fix 日志修复、README 重写。
- **CHANGELOG.md 新建**：AGENTS.md 第四节首次落地，用户向口吻，含 v0.1.0 追溯段。
- **流程改进**：CHANGELOG + devlog 分段**先入 tag 再构建**（v0.1.0 时收官 docs 在 tag 后）；构建/验证/冒烟/发仓链路同 v0.1.0（见 `2026-09-17-release-v010.md`「下次发版流程」），全部复用无障碍。

## 真机冒烟（覆盖安装 alpha.9 → 0.1.1，同签名）

- versionName 0.1.1 / versionCode 56 实证；开机链 20:15:33 清理残留 → 20:15:34 热更新检查 → 20:15:37 RUNNING，**全程 4 秒**（CDN UPTODATE）。
- wrapper/gui 在岗、status API 正常；runner 未启动（挂机由用户手动开启）。
- 设备上现在跑的是 **release 版**（run-as 失效，深调需临时装回 debug 包）。

## 未决事项（等自然触发，勿主动追）

- **GemsFarming 'MRT' 崩溃复验**：等自然调度（failure 延迟机制），同源 OCR 栈已证，预期已愈。
- **CDN UPDATED 路径真机首验**：等上游下次推 commit；观察点 = session.log 出现「重放本地补丁」阶段行。
- goal（roadmap-v3 主线）仍 blocked，阶段五 -1/-2/-5 随用户安排。

## 发版操作要点（复用）

- GitHub 直连不稳，push/gh 走代理：`git -c http.proxy=http://127.0.0.1:7897 push` / `HTTPS_PROXY=http://127.0.0.1:7897 gh release create ...`（一次性参数，勿写入 git config）。
- 构建：`cd app && JAVA_HOME='D:\VSCodeCache\shizku-m\build-env\jdk-17.0.2' GRADLE_USER_HOME='<repo>\.tmp\gradle-home' cmd //c 'gradlew.bat assembleRelease --console=plain'`（~1.5min）。
