# Handoff 2026-09-16 · M4-d 收官（设置/构建死账清仓）+ 演示前置体栓全绿

> 新建对话或上下文压缩后：先读本文与 `docs/roadmap-v3.md`，再从「下一步」继续。
> 上一篇：`2026-09-16-m4c.md`（阶段四应用内侧条目）。

## 当前状态

- **阶段四应用内侧全落地**（M4-a/b/c）+ **M4-d 死账清仓**：M2-b 遗留②③④⑤全核销——死 pref×7 删、分辨率安慰剂连根拔（`setVirtualDisplayResolution` 零调用实锤 placebo）、`runner/` 包连包删、okhttp/tracing/baselineprofile/markwon/jna/sentry/angus 等依赖与 toml 死条目清、`azurpilot.android.benchmark` 约定插件删。设置页「其他设置」现只剩启动模式。
- **「开始挂机」演示前置体检全绿**：游戏 `com.bilibili.azurlane` 在装 ✓；guest 上游配置文件 PackageName 一致 ✓、Serial=azurpilot ✓；VD #17 在、桥通、wrapper 可达 ✓；runner 未启动。
- 设备现状：最新包在跑，proot 会话 RUNNING。
- git：M4-d 代码+账册已 push main。

## 下一步

1. **用户演示「开始挂机」端到端**（阶段四 DoD 收官项，必须用户在场：游戏在 VD，悬浮窗点开始 → AzurPilot 控制游戏、日志上板）。
2. 演示过 → **阶段五**（roadmap §7）：多 ROM 矩阵（手上或还有 shizuku-m 测试机 GT Pro 可作第二台）、长挂机 2~3h + 息屏、断网容灾、shizuku-m 产品化（重立 fork/tag/图标）、Release 签名、三仓公开、README（支持机型清单 + 分工说明 + shizuku-m 获取方式回填 + 官方版冲突说明）。
3. 随时可穿插的待用户项：真机重启验证（阶段三 DoD ③）；油数验收（游戏点到出击菜单页）；appId 身份（上游 fork→AzurPilot 命名）。

## 关键路径与命令

- 构建/安装：`development.md`；adb 只吃 Windows 路径，安装判据=输出含 `Success`。
- 验证口：`adb forward tcp:22400 tcp:22400`（+22267）→ curl `/status`、`/`。
- guest 配置核查：`adb shell run-as com.aliothmoon.azurpilot cat files/rootfs/opt/azurpilot/config/<上游配置文件>`。

## 关键决策与注意

- **VD 分辨率不做用户可选项**：恒 1280×720（`DefaultDisplayConfig`），桥 screencap 协议钉死；要改得连桥协议与 guest 校验一起动。
- DataStore 里被删 pref 的旧值成孤儿键，无害不清。
- guest 时间戳=UTC（debug.md 有条目）；真机纪律（不私按「开始挂机」/不代点游戏/tap 前核坐标）不变；git 全预授权（release/tag 逐次）。
