# Handoff 2026-09-16 · M3-a 收官（首启解压流水线真机全绿）

> 新建对话或上下文压缩后：先读本文与 `docs/roadmap-v3.md`，再从「下一步」继续。
> 上一篇：`2026-09-16-m2.md`（阶段二收官）。

## 当前状态

- **阶段二全部完成**（M2-a~d，DoD 6/6 真机验证，清场完毕）；账册同步至 `106578f`。
- **M3-a 完成（`f8d8cbe` 已 push）**：首启 rootfs 解压流水线真机全绿——
  - `provision/RootfsProvisioner`（状态机六档）+ `ui/setup/ProvisionScreen` + AppRoot 门（未 Ready 整屏接管；开发包未内置资产可跳过，仅 DEBUG）。
  - 解压：commons-compress + tukaani xz 纯 Java 流式；**目标=内部 filesDir**（/sdcard 无符号链接+noexec，debug.md 同日条目）；符号链接 740 全落地、硬链接物化、+x 位保留、zip-slip 防护、≥2GB 磁盘校验、256KB 节流进度。
  - 版本闸门：assets `rootfs/BUILD_MANIFEST`（入库）vs marker `.provisioned`（=0.1.0）。
  - 真机：~90s 解 971MB、二启秒过门、python3→3.12 +x、BUILD_MANIFEST 可读（决策 #10）。
  - **幻影键已修正**为 Spike C 实证对（`settings_enable_monitor_phantom_procs false` + `device_config max_phantom_processes 2147483647`），设备侧确认写入；m0 两旧键实测无效已删。
- 资产约定：`app/app/src/main/assets/rootfs/rootfs.tar.xz`（v4 artifact，gitignore 不入库，APK 392MB）+ `BUILD_MANIFEST` 入库。换新 rootfs = 换这两个文件。
- 设备现状：app 已装（含 rootfs），VD/球链路正常；M1-d harness WebUI 后台任务 bash-r6h84em7 保活中（22267）；游戏未开（油数验收待用户点到出击页）。

## 下一步（M3-b：FGS 拉 proot + 热更新 + wrapper）

1. **libproot.so + busybox 进 app jniLibs**（取自 spike/a-proot-exec 的 nld 资产或 build-env；含 argv0 shim 与 dynstr 改写件，复用 `spike/a-proot-exec/tools/patch-dynstr.py`）。
2. 前台服务 ProcessBuilder 拉起 proot → 自愈清锁（proot 残留锁/临时目录）→ **Gitee 热更新**（只拉 AzurPilot 源码不动依赖；断网超时降级不阻塞）→ wrapper.py → AzurPilot（Spike D：薄 runner import `AzurLaneAutoScript.loop()`，killpg 管进程组；兜底 `python azurpilot.py`）。
3. M3-c：生命周期防护（stdin 管道破裂自尽 + 进程组全杀；划卡进程归零）+ 重启引导（Shizuku 不活跳决策 #11 图文链）。

随时可穿插的待用户项：M1-d 油数验收（用户把游戏点到出击菜单页）；appId 身份（上游 fork→AzurPilot 命名，用户定夺）。

## 关键决策与注意

- 构建命令不变（见 `development.md`）；APK 现 392MB，install 约 40s（WiFi adb）。
- **真机操作纪律**：tap 前核旋转与目标当前帧坐标（debug.md 2026-09-16）；不私自锁屏；不代点游戏；VD 纪律见 `AGENTS.md`；悬浮窗消失排查口诀（mViewVisibility 0x8=app 藏/0x0+策略位=系统藏）。
- git 全预授权本次长任务有效（release/tag 逐次授权）。
