# Handoff 2026-09-15 · M1-c GHA 构建迭代中（首跑 404 已修，第二跑在飞）

> 新建对话或上下文压缩后：先读本文与 `docs/roadmap-v3.md`、`devlog.md` 顶部条目，再从「下一步」继续。
> 历史 handoff：`2026-09-15.md`、`2026-09-15-repo.md`（建仓）、`-spike-a.md`、`-spike-c.md`、`-spike-e.md`、`2026-09-15-m1.md`（M1 本地就绪版，产物细节全在其中，不重述）。

## 当前状态

- **阶段〇全部收官**（A/B′/C/D/E 全 ✅）。手势劫持硬约束已落 AGENTS.md / debug.md / roadmap 阶段二。
- **M1 产物已进 main**：`b263dfb`（M1 全量）→ `e85255d`/`a1c9e96`（构建修复）→ `e233630`（cached-property 补缺）。仓：`github.com/Shinarin/AzurPilot` PUBLIC。
- **GHA rootfs.yml 第四跑（`e233630`）绿**；artifact 后台下载至 `.tmp/rootfs-dist-v4`（交付基准）。构建链已定型（5 分钟内出包）。
- **M1-d 真机复验 3 项已过 2.5 项**：
  - ③ BUILD_MANIFEST 可读 ✅；① WebUI 单命令拉起 200 ✅（shell 域 proot `gui.py`，bash-r6h84em7 保活，PC 浏览器 127.0.0.1:22267 可看）；
  - ② 油数 100 次：**链路已机械验证**（screencap→frames bind→Digit→in-proc OCR 全通），**只差游戏画面——等用户把游戏点到出击菜单页**，到位后：实帧目测原生油区坐标 → 100 帧连拍 → 跑 `/opt/azurpilot/m1d-oil-probe.py <x1,y1,x2,y2>` 看 ≥98%。
- **设备侧布局**：shell 域 harness @ `/data/local/tmp/`（nld/ 含 proot+busybox 裸名软链、rootfs/、frames/、proot-tmp/）；run-as 域**禁 socket 不可用作 harness**（debug.md 已记）；rootfs 内已热修 cached-property 2.0.1 + 静态 resolv.conf。
- **关键坑（debug.md 新增 4 条）**：busybox tar 解 ubuntu 硬链接必炸（PC 重打包去硬链接）；run-as 禁 socket；libbusybox.so 须裸名软链；shell 域禁路径式 AF_UNIX（harness 用 sitecustomize 换 127.0.0.1，仅测试用）。
- 幻影缓解双命令保持开启；不做锁屏/息屏测验；不碰 VD SYSTEM_DECORATIONS flag。

## 下一步

1. **【等用户】游戏到出击菜单页** → 实帧目测原生油区 → `screencap` 100 帧 → proot 跑 `m1d-oil-probe.py` → ≥98% 则 ②✅、M1-d 全绿、**阶段一 DoD 达成**。
2. 核对 `.tmp/rootfs-dist-v4` artifact（sha/门禁输出）入 devlog；设备侧 rootfs 可与 v4 对齐（重解或热修已等价）。
3. 阶段一收官汇报 → **阶段二**（工作底稿已备：`docs/stage2-fork-inventory.md`——桥须在特权进程内 Kotlin 重写 5 端点；fork 6 处未提交 WebView 资产先固化；`local.properties` 死路径先修；VD flag 已合规）。

## M1-d runbook（旧方案，已作废——实际执行见「当前状态」与 devlog M1-d 条目）

> 作废原因：run-as（runas_app 域）禁 socket、busybox tar 解不了 ubuntu 硬链接。
> 现行 harness = **shell 域** `/data/local/tmp/`（nld + rootfs + frames + proot-tmp），命令形态见 devlog「M1-d（上/中）」。

<details><summary>旧 run-as 方案存档</summary>

设备现状：spikea 已装（`com.azurpilot.spikea`），nld 含 libproot/libproot-loader/libandroid-shmem/libtalloc/libbusybox（注意 busybox 须软链成裸名调用，见 debug.md）；/data/local/tmp 可用、/data 余量 91G；WiFi adb 通道在线。

```bash
export MSYS_NO_PATHCONV=1
S=192.168.50.190:5555
# 0) 取产物（PC）：gh run download <RUN> -n rootfs -D .tmp/rootfs-dist/
# 1) 推包
adb -s $S push .tmp/rootfs-dist/rootfs.tar.xz /data/local/tmp/
# 2) app 域内解包（busybox 裸名软链；cat 重定向把包搬进 app 私有目录）
adb -s $S shell 'run-as com.azurpilot.spikea sh -c "
  NLD=$(dirname $(dirname /data/app/*/com.azurpilot.spikea-*/base.apk 2>/dev/null | head -1))/lib/arm64  # 若 glob 失效则先 pm path 推出
  mkdir -p files/bin files/rootfs files/tmp
  ln -sf \$NLD/libbusybox.so files/bin/busybox
  export LD_LIBRARY_PATH=\$NLD
  cat /data/local/tmp/rootfs.tar.xz > files/rootfs.tar.xz
  ./files/bin/busybox xzcat files/rootfs.tar.xz | ./files/bin/busybox tar -x -C files/rootfs
  ./files/bin/busybox chmod -R a+x files/rootfs   # proot 解释器 +x 硬要求（Spike A 坑）
"'
# 3) proot 拉起探针（Spike A 同款环境注入）
adb -s $S shell 'run-as com.azurpilot.spikea sh -c "
  NLD=<同上>; export LD_LIBRARY_PATH=\$NLD PROOT_LOADER=\$NLD/libproot-loader.so PROOT_TMP_DIR=\$PWD/files/tmp
  \$NLD/libproot.so -w / -r \$PWD/files/rootfs -b /dev:/dev /usr/bin/python3 --version
"'
# 4) 验收三件套
#  ① 起 WebUI：proot 内 cd /opt/azurpilot && python wrapper.py（或 runner）→ adb forward tcp:22267 tcp:22267 → PC 浏览器开 127.0.0.1:22267（阶段一 DoD）
#  ② 油数 100 次 ≥98%：游戏在出击页，rootfs 内跑 /opt/azurpilot/spike-f-ocr-gate.py（--real-dir 或实时截图口径，照 m0-archive/termux/ocr_probe.py）
#  ③ cat /opt/azurpilot/BUILD_MANIFEST
# 5) B′ 顺带：rootfs 内若有静态 adb，首连 5555 盯新 key 授权弹窗
```

注意：Ubuntu rootfs 的 PT_INTERP=`/lib/ld-linux-aarch64.so.1` 走 proot 另一分支（Spike A 报告 §6 遗留项 1——已在 shell 域 harness 实证通过：python3.12.3/import/WebUI 全通），探针失败先查 +x 与 PROOT_LOADER，再怀疑 SELinux；`PROOT_NO_SECCOMP=1` 是已验证降级开关。

</details>

## 关键决策与注意

- **git 授权（2026-09-15 用户指令）**：**本次长任务期间 git 操作全部预授权**（含 commit/push），构建迭代与里程碑产物直接推，不再逐次报备。发版（release/tag）不在其列，仍需逐次授权。
- 真机口径：`export MSYS_NO_PATHCONV=1` 后 adb shell；USB `AVAY025422002864`（可能离线）+ WiFi `192.168.50.190:5555`；主机路径 `cygpath -m`；Windows Python `C:\Python314\python.exe`；项目 venv `.tmp/venv-ocr/venv/Scripts/python.exe`。
- OCR 通道序约定：整屏 3ch 彩图 BGR/RGB 未显式定义，留 M1-d 真机定案并补 rpc.py 注释。
- runner.py 的 AzurPilot 类在 `azurpilot.py:18`；`seed_config.py` 与新版 master template.json 键兼容性未验证（M1-d 顺带看）。
- CI 门禁无中文字体 → 合成图用例 SKIP 属预期；真机油数才是 DoD 主口径。
- wrapper 不碰 AzurPilot ProcessManager；WebUI 启停按钮与悬浮窗并存问题留阶段三决策（候选：同进程 uvicorn 直调 `ProcessManager.get_manager()`）。
