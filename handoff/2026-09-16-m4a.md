# Handoff 2026-09-16 · M4-a 收官（悬浮窗直连 wrapper：调度器控制面 + 日志板）

> 新建对话或上下文压缩后：先读本文与 `docs/roadmap-v3.md`，再从「下一步」继续。
> 上一篇：`2026-09-16-m3c.md`（阶段三收官）。

## 当前状态

- **M4-a ✅**：悬浮窗「开始挂机/停止挂机」映射 wrapper 薄 HTTP（POST /start、/stop，22400）+ AzurPilot 调度器状态行（未就绪/运行中·pid/已停止）+ 半透明日志板（GET /logs?tail=80，自动沉底）。真机面板渲染与 curl /status 逐字段一致。
- **双头管理定案**：不做 Spike D 的 in-proc uvicorn 重构（改动已验证链路，风险大于收益）。悬浮窗=唯一控制面；WebUI 启停按钮（ProcessManager 通道，双跑抢设备）「不要用」——文档警告先行，阶段五评硬化。
- 设备现状：最新包在跑，proot 会话 RUNNING（VD #15、桥通、球在），runner 未启动（「开始挂机」端到端**留用户在场演示**——会真拉起 AzurPilot 操作游戏）。
- git：M4-a 代码+账册已 push main。

## 下一步（阶段四剩余）

1. **「开始挂机」端到端演示**（用户在场）：游戏在虚拟屏 → 悬浮窗点开始 → AzurPilot 控制游戏、日志上板。
2. 阶段四余项（roadmap §6）：shizuku-m 未装/未激活引导页完善（README + 应用内图文）；「高频操作走悬浮窗、完整配置走 WebView」分工文案落 UI/README。
3. 随时可穿插的待用户项：真机重启验证（阶段三 DoD ③，需授权配合）；油数验收（游戏点到出击菜单页）；appId 身份（`com.aliothmoon.azurpilot`→AzurPilot 命名，用户定夺）。

## 关键路径与命令

- 构建/安装：`development.md`；adb 只吃 Windows 路径，安装判据=输出含 `Success`。
- 验证口：`adb forward tcp:22400 tcp:22400`（+22267）→ curl `/status`、`/`。
- 新代码：`app/app/src/main/java/com/aliothmoon/azurpilot/proot/AzurPilotRunController.kt`（4s 轮询 + busy 置位 + Mutex 互斥；POST 读超时 12s 覆盖 /stop 的 SIGTERM→3s→SIGKILL）；面板 `overlay/OverlayPanel.kt`（日志板 weight(1f) + `LaunchedEffect(linesCount, lines.size)` 沉底）。
- 日志板语义：wrapper /logs 按 mtime 取 `log/` 最新 *.txt——调度器跑起来是它的 `{date}_<配置名>.txt`，空闲时是 gui 启动日志（恰是排障现场）。拉取策略=**可达即拉**（loopback 开销可忽略）。

## 关键决策与注意

- **guest 时间戳=UTC**（proot 无 TZ）：/status 的 `*_started_at` 与 AzurPilot 日志行时间均比设备 CST 慢 8h——判会话新旧看 ps 进程树，别看绝对时间（debug.md 有条目）。
- 真机操作纪律不变：tap 前核旋转与目标当前帧坐标；不私自锁屏/息屏/重启；不代点游戏；**不私按「开始挂机」**。VD 纪律见 `AGENTS.md`。
- git 全预授权本次长任务有效（release/tag 逐次授权）。
- assets/azurpilot 与 rootfs/{overlays,seeds,patches} 同源两份：改动先改 rootfs/ 再拷 app assets（上次验证 ALL_IN_SYNC）；本次 M4-a 纯 Kotlin，未触及。
