# Handoff 2026-09-16 · M4-b/c 收官（官方版冲突引导 + 开屏淡出 + 分工文案）

> 新建对话或上下文压缩后：先读本文与 `docs/roadmap-v3.md`，再从「下一步」继续。
> 上一篇：`2026-09-16-m4a.md`（悬浮窗控制面 + 日志板）。

## 当前状态

- **阶段四应用内侧条目全落地**：① 开屏探针→自动载入+淡出开屏（M3-b 机制 + M4-c `pageReady` 开屏层补完，不再闪错误脸）✅；② 悬浮窗 start/stop 映射 wrapper 薄 HTTP（M4-a）✅；③ 日志板（M4-a）✅；④ shizuku-m 引导页应用内侧（M4-b：未安装/未激活/未授权沿用 + **官方版冲突新档** OfficialConflict→去卸载闭环）✅；⑤ 分工文案（M4-c 面板 caption）✅。
- **阶段四 DoD 只剩端到端演示**：游戏（虚拟屏）上点悬浮窗「开始」，AzurPilot 立即控制游戏、日志上板——**必须用户在场**（会真操作游戏），我不能私按。
- 设备现状：最新包在跑，proot 会话 RUNNING（VD #17、桥通、球在），runner 未启动。
- git：M4-b/c 代码+账册已 push main。

## 下一步

1. **用户演示「开始挂机」端到端**（阶段四 DoD 收官项）。前置：游戏已开（在 VD 里）。
2. 演示过 → 阶段四收官，进**阶段五**（roadmap §7）：多 ROM 矩阵、长挂机 2~3h + 息屏、断网容灾、shizuku-m 产品化（重立 fork/tag/图标）、Release 签名、三仓公开、README（含支持机型清单 + 分工说明 + shizuku-m 获取方式回填）。
3. 随时可穿插的待用户项：真机重启验证（阶段三 DoD ③）；油数验收（游戏点到出击菜单页）；appId 身份（上游 fork→AzurPilot 命名，用户定夺）。

## 关键路径与命令

- 构建/安装：`development.md`；adb 只吃 Windows 路径，安装判据=输出含 `Success`。
- 验证口：`adb forward tcp:22400 tcp:22400`（+22267）→ curl `/status`、`/`。
- M4-b 关键代码：`privileged/ShizukuReadiness.kt`（OfficialConflict 档 + ShizukuFlavor）、`PermissionManager.probeShizukuStage`（flavor 优先于 NeedAuth；label 含 "Shizuku-m" 判 MOD）、`uninstallShizuku`（ACTION_DELETE）。
- M4-c 关键代码：`ui/azurpilot/AzurPilotScreen.kt`（`pageReady` + AnimatedVisibility fadeOut 开屏层，真失败才给错误面板）；`overlay/OverlayPanel.kt` caption `overlay_azurpilot_hint`。

## 关键决策与注意

- **官方版冲突验证限制**：真机冲突分支未演（需卸 shizuku-m 装官方版，破坏性），靠逻辑评审+烟测；留阶段五多 ROM 矩阵补。
- **guest 时间戳=UTC**（proot 无 TZ），比设备 CST 慢 8h（debug.md 有条目）。
- 真机操作纪律不变：tap 前核旋转与当前帧坐标；不私自锁屏/息屏/重启；不代点游戏；**不私按「开始挂机」**。VD 纪律见 `AGENTS.md`。
- git 全预授权（release/tag 逐次授权）；assets/azurpilot ↔ rootfs 同源两份（本次未触及）。
