.tmp/
.uv-cache/
.venv/
__pycache__/
/config/
debug/
dist/
node_modules/
azurpilot.log
*.log

# v3 阶段：本地归档（上游 fork 与桌面副本，留本地不进公开仓）
m0-archive/

# Android/Gradle 构建产物（可重建）
.gradle/
build/
.kotlin/
local.properties
*.iml
.idea/

# spike 产物：APK 二进制可重建不入库；dist/logs 文本证据保留
!spike/**/dist/
spike/**/dist/*.apk

# v3 阶段一：rootfs/ 资产必须进仓，解除上方通用目录名规则（build/、config/）的误伤
!rootfs/build/
!rootfs/patches/module/config/

# 阶段三：rootfs 资产随包内置（300MB+ 不入库，构建前从 rootfs-dist 拷入）；BUILD_MANIFEST 入库
/app/app/src/main/assets/rootfs/rootfs.tar.xz
/app/app/src/main/assets/rootfs/BUILD_MANIFEST
/keystore/

handoff/*
!handoff/latest.md

# ===== ↑ 以上同步自 .gitignore（「从 .gitignore 同步」只重写以上部分）=====
.git/
.hg/
.svn/
bower_components/
jspm_packages/
site-packages/
venv/
coverage/
htmlcov/
lcov-report/
cmakefiles/
cmake-build-*/
bazel-*/
pods/
deriveddata/
storybook-static/
playwright-report/
test-results/
allure-results/
allure-report/
cdk.out/
*.egg-info/
*.dist-info/
eggs/
pip-wheel-metadata/
wheels/
# ----- ↑ 以上为 ZCode 默认排除规则（自定义规则请写在本行下方，不会被同步/恢复改动）-----
# 自定义规则写在下方（本行提示可删除）
