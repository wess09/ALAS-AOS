# Handoff 2026-09-15 · 阶段一 M1 本地就绪，等 push 授权

> 新建对话或上下文压缩后：先读本文与 `docs/roadmap-v3.md`，再从「下一步」继续。
> 历史 handoff：`2026-09-15.md`、`2026-09-15-repo.md`（建仓）、`-spike-a.md`、`-spike-c.md`。

## 当前状态

- **阶段〇全部收官**（A/B′/C/D/E 全 ✅，F 并入 M1）。手势劫持事件已彻查收口，硬约束落 AGENTS.md / debug.md / roadmap 阶段二。
- **M1 本地产物全部就绪**（进仓未 commit，等用户授权 push 后触发 GHA 构建）：
  - `rootfs/patches/`（19 文件，m0 补丁集，已剔除 rpc.py 桥接版与 __pycache__）、`rootfs/seeds/deploy.yaml`（fullcn 镜像 + 更新器七键锁定，与 template-linux-cn 行结构一致）+ `seed_config.py`、`rootfs/shims/jellyfish.py`、`rootfs/models/ocr/`（三件套 sha 一致）。
  - `rootfs/overlays/module/ocr/rpc.py`（361 行，in-proc onnxruntime PP-OCR，m0 接口逐字保持；**本机真测一轮通过**：合成数字 13/13、2D 堆叠 2/2、整屏 det+rec 23 框合理、并发 8×20 零错误、负路径 RequestHumanTakeover 正确、门禁阈值自验 PASS/FAIL 正确）。
  - `rootfs/overlays/wrapper.py`（127.0.0.1:22400 薄 HTTP：status/start/stop/logs + killpg 进程组 + stdin 破裂自尽 + 单实例锁）、`runner.py`（`from azurpilot import AzurLaneAutoScript` → loop()，MAAAL_ALAS_CONFIG 可改实例名）。
  - `rootfs/build/build-rootfs.sh`（bash -n 过；pip 层 = **m0 setup_env.sh 现代化宽松集 + onnxruntime**，非 AzurPilot 钉版——aarch64+py3.12 大面积死链已核实；装后 import 硬门禁 fail-fast；BUILD_MANIFEST 双份；挂载三重防线）+ `spike-f-ocr-gate.py`（CI/真机两用，≥98% exit 0）。
  - `.github/workflows/rootfs.yml`（ubuntu-24.04-arm，dispatch + push 触发，chroot 跑门禁，artifact 14 天）。
- `.gitignore` 修了 `build/`、`config/` 通用规则对 `rootfs/` 的误伤（否定规则已验）。
- 设备：游戏 `com.bilibili.azurlane` 在机上（M1-d 前提 ✓）；WiFi 通道 192.168.50.190:5555 可用；幻影缓解双命令保持开启。

## 下一步

1. **【等用户授权】** commit + push M1 产物 → 触发 GHA 首次构建。盯：宽松依赖集实际解析版本、import 硬门禁输出、rootfs.tar.xz 体积（目标 ~250MB，>400MB 告警）、门禁在 CI 的 PASS（无中文字体时合成图用例记 SKIP 不影响 PASS）。
2. GHA 产物回来后 **M1-d 真机复验**：解压 rootfs → proot 拉起（复用 spike A harness，坑：`proot -b /dev:/dev`、全量 +x）→ 单命令起 WebUI（阶段一 DoD）+ 油数 100 次 ≥98%（需用户开游戏到出击页配合截图/实时口径）+ B′ 遗留的新 key 授权弹窗生产实景验收（rootfs adb 首连时）。
3. 阶段一 DoD 全过后 → 阶段二（上游 fork fork 复活做减法，遵守 VD flag 硬约束）。

## 关键决策与注意

- **push/发版红线**：M1 产物 commit+push 待用户明确指令；本次 push 会触发 GHA 构建（公共仓免费 ARM64 runner）。
- OCR 通道序约定：生产路径（2D 灰度行图 rec）与通道序无关；整屏 3ch 彩图的 BGR/RGB 约定**未显式定义**（当前实测效果 OK），留 M1-d 真机验证时定案并补 rpc.py 注释。
- runner.py 的 AzurPilot 类在 `azurpilot.py:18`（无 AzurLaneAutoScript.py 文件），官方无头入口 `azurpilot.py:621-623`。
- 构建 DoD 口径：Termux/侧载环境手动解压、离线单命令拉起 AzurPilot WebUI、BUILD_MANIFEST 可读。
- CI 门禁无中文字体（fonts-noto-cjk ~100MB 未装），合成图用例 SKIP；真机油数才是 DoD 主口径。
- wrapper 不碰 AzurPilot ProcessManager（双头管理风险），WebUI 启停按钮与悬浮窗并存问题留阶段三决策（候选：同进程 uvicorn 直调 `ProcessManager.get_manager()`）。
