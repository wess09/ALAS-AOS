# Handoff 2026-09-16 · M3-c 收官（生命周期防护全绿 + 重启引导落地）

> 新建对话或上下文压缩后：先读本文与 `docs/roadmap-v3.md`，再从「下一步」继续。
> 上一篇：`2026-09-16-m3b.md`（M3-b proot 会话 + 热更新）。

## 当前状态

- **阶段三 DoD 三项中两项全绿**：① 开屏热更新→外部浏览器开 127.0.0.1:22267 ✅（M3-b）；② 划掉 App 无残留 Python/proot/桥进程 ✅（M3-c：杀 Java 进程 ≤3s 全树自尽 + force-stop 零残留双路径实证）。
- ③ 重启手机经引导恢复可挂机状态：**机制+文案已落地**（readiness 弹窗 NotRunning 档 = 决策 #11 手动链图文案），**真机重启端到端验证待用户授权**。
- **设备现状**：app 最新包在跑（M3-c 版），生产 proot 会话在跑（wrapper+gui，22400/22267 可达）；游戏未开（油数验收待用户点到出击菜单页，走生产链）。
- git：`916783a`（M3-b）+ 本次 M3-c 提交已 push 到 `github.com/Shinarin/AzurPilot` main。

## 下一步（阶段四：交互闭环与体验）

1. HTTP 探针轮询 22267，就绪后 WebView 自动载入并淡出开屏动画（m0 `vh` 注入修复已在 AzurPilotScreen）。
2. 悬浮窗 start/stop 映射 wrapper 薄 HTTP（POST /start、/stop，22400）；悬浮窗半透明日志板（GET /logs?tail=N）。
3. **双头管理决策**（WebUI 启停按钮 vs wrapper/悬浮窗）：Spike D 候选=wrapper 同进程 uvicorn 直调 `ProcessManager.get_manager()`；兜底=接受 WebUI 按钮失效并文档警告。
4. shizuku-m 未装/未激活/与官方版冲突的引导页完善（README + 应用内图文）。
5. 分工定调：高频操作走悬浮窗，完整配置走 WebView。

随时可穿插的待用户项：**真机重启验证**（重启手机→开 shizuku-m 点启动→回 AzurPilot→确认自动恢复挂机态；需用户授权并配合）；油数验收（游戏点到出击菜单页）；appId 身份（上游 fork→AzurPilot 命名，用户定夺）。

## 关键路径与命令

- 构建/安装：`development.md`；**adb 只吃 Windows 路径**，安装判据=输出含 `Success`。
- 验证口：`adb forward tcp:22400 tcp:22400` + `tcp:22267` → curl `/status`、`/`；会话日志 `/sdcard/Android/data/com.aliothmoon.azurpilot/files/log/proot/session.log`；guest 日志 `run-as com.aliothmoon.azurpilot … files/rootfs/opt/azurpilot/log/`。
- 划卡压测手法：`run-as com.aliothmoon.azurpilot kill <appPid>`（只杀 Java 进程）→ 3s 后 `ps -A | grep -E "proot|python"` 应为空。
- wrapper 端点：GET /status、POST /start、POST /stop、GET /logs?tail=N（薄 HTTP 22400；MAAAL_WEBUI=0 关 WebUI）。

## 关键决策与注意

- **真机操作纪律不变**：tap 前核旋转与目标当前帧坐标；不私自锁屏/息屏/**重启**；不代点游戏；VD 纪律见 `AGENTS.md`。
- git 全预授权本次长任务有效（release/tag 逐次授权）。
- assets/azurpilot 与 rootfs/{overlays,seeds,patches} 同源两份：改动先改 rootfs/ 再拷 app assets（已验证 ALL_IN_SYNC）。
- RUNNING 语义=wrapper+WebUI 双端口 200（debug.md 同日条目）；proot 九件套在 `prootLibs/`（jniLibs 是 运行框架 拉取件目录，gitignore）。
