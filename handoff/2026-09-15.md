# Handoff 2026-09-15 · 路线规划阶段收官

> 新建对话或上下文压缩后：先读本文，再读 `docs/roadmap-v3.md`，然后从「下一步」继续。

## 当前状态

路线规划完成，`docs/roadmap-v3.md` 已产出，**等待用户确认**。确认前不动手写阶段〇。

## 已完成

- m0 全部内容归档 `m0-archive/`（2026-09-01）。
- 三轮拷问（Q1–Q13）全部落锤 → 13 项决策，见 `docs/roadmap-v3.md` 第 0 节。
- 两份事实核查（要点已吸收进 roadmap-v3 附录 B）：
  - m0 归档：proot-distro 否决理由在 v3 架构下全部化解；OCR 桥接根因 = cnocr/mxnet aarch64 死链；桥性能 p50=0.109s；后台挂机为用户拍板的硬需求。
  - shizuku-m：v13.6.0 + 3 commits，`tcpip:5555` 通道；跨开机需在线激活；Apache-2.0 但图标衍生自上游禁用素材（对外分发前须换）。
- v2 与档案的矛盾已修正：「魔改 Shizuku 可直接拉起」实为 tcpip:5555 通道且跨开机仍需激活；「初版五阶段路线图」档案查无此物。

## 下一步

1. 用户确认/修订 v3。
2. 确认后：
   - 重写 `AGENTS.md`「当前状态」节为新阶段规则（注意「工程约定」里 `vendor/上游 fork` 路径已过时，现为 `m0-archive 里的 fork 基线`）；
   - 阶段〇 Spike A 开工：`libproot.so` 放 jniLibs 走 nativeLibraryDir exec，检查 Android 15+ 16KB 页对齐；备选 targetSdk 28。

## 关键决策与注意

- 控制面 = m0 桥（TCP 22300 五端点，10s 心跳，全局串行锁），不重写；OCR = in-proc PP-OCR（rpc.py shim 接口保留）。
- shizuku-m 不内置（用户 Q10 选 B）；引导需写明与官方 Shizuku 互斥。
- 每次开机不可约手动链：开无线调试 → shizuku-m 点启动（首次另加一次性配对）。
- 真机调试闭环不变：设备 `AVAY025422002864`，adb shell 前必须 `export MSYS_NO_PATHCONV=1`。
- 不做 git 提交 / push / 发版，除非用户明确指令。
