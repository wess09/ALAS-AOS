# 2026-09-20 · 品牌重塑：项目 → AzurPilot（仓库/App 改名 + azurpilot 残留清扫）

> 上一篇：`2026-09-19-release-v012.md`。本篇记品牌重塑全程，是跨会话接力的最新状态锚点。

## 最终状态（事实）

- **项目名**：AzurPilot → **AzurPilot**（README/AGENTS.md/agents.md 镜像/活跃 docs 已全量切换）。
- **GitHub 仓库**：`Shinarin/AzurPilot` → **`Shinarin/AzurPilot`**（gh repo rename 已执行并 view 验证；旧 URL 由 GitHub 自动重定向）。本地 `origin` 已 set-url 到 `https://github.com/Shinarin/AzurPilot.git`。
- **App 名称**：更早的旧名 → **AzurPilot**（`values/strings.xml` + `values-en/strings.xml` 的 app_name / log_export_subject / notification_test_message，XML 校验通过）。
- **App 整体身份断代（2026-09-20 晚，用户确认接受"无法覆盖更新"）**：
  - applicationId `com.aliothmoon.azurpilot` → **`io.github.shinarin.azurpilot`**（build-logic `BASE_APPLICATION_ID` 单点改动；manifest providers 走 `${applicationId}` 占位自动跟随；运行时全走 `context.packageName` 动态获取，无硬编码；Java 包名 `com.aliothmoon.azurpilot` 保留为内部代码身份——appId 与 namespace 解耦，零代码 churn）。
  - 签名整体换发：新 keystore `keystore/azurpilot-aos-release.jks`（CN=AzurPilot，RSA 4096，10000 天，SHA-256 `f52207ec…`），local.properties 已切换；旧 `azurpilot-release.jks`（CN=更早的旧名，v0.1.2 及更早历史签名）退役但保留在同目录，**勿删**。
  - **断代影响**：新旧包名+签名均不同 → 新版是全新应用，老版本无法覆盖升级，需**卸载旧版再装新版**（AzurPilot 配置随卸载清除，需提前导出）；新版首启需重新授予 Shizuku。
  - **验证**：`assembleRelease` 全绿（2m04s），aapt badging package=`io.github.shinarin.azurpilot` / label=`AzurPilot` / arm64-v8a / versionCode 62 / versionName 0.1.3-alpha.1，apksigner V2 CN=AzurPilot。
- **下版 APK 文件名**：`AzurPilot-v<版本号>-android-arm64.apk`（命名是发版流程的手工改名步骤，无构建配置；README 下载行已同步）。
- **下版 AzurPilot WebUI 桥接选项显示名**：`AzurPilot 桥` → `AzurPilot 桥`（regen_args.py 双源 cmp 一致，每次启动重生 args.json 时生效）。

## 刻意保留（勿当残留误删）

| 项 | 为什么保留 |
|---|---|
| 旧签名 keystore 文件 `keystore/azurpilot-release.jks` | v0.1.2 及更早版本的历史签名凭证，退役留档（找回历史包/审计用），构建已不再引用 |
| ~~AzurPilot 桥接 method 值 `azurpilot`、类 `AzurPilot`、Python 日志前缀~~ | **已被 2026-09-20 晚的标识符断代整改取代**：`azurpilot`→`azurpilot`、`AzurPilot`→`AzurPilot`、`MAAAL_`→`ALASAOS_` 全量更名并真机验证通过；配置兼容顾虑随 appId 断代（全新安装=全新播种）自然消解 |
| Java 包名 `com.aliothmoon.azurpilot`（源码 namespace）与 fork 内部类名（`MaaFrameworkRunnerPort`、测试样例注释、stage2-inventory/debug.md 的史实引用） | 内部代码遗产，非品牌；appId 已独立换新（io.github.shinarin.azurpilot），namespace 与 appId 解耦是 Android 常规做法 |
| `app/README.md`（上游 上游 fork README 存档，头部已声明"不描述本仓库产品"） | 架构来源存档；其中 INTEGRATION.md 死链已划线标注 |
| `m0-archive/`（含 vendor/上游 fork @ b2b0f54 基线） | 只读归档 |
| 本地工作目录名 `D:\VSCodeCache\azurpilot-azurpilot` | local.properties 里 keystore 绝对路径依赖它；要改需同步改配置，用户未要求 |
| BuildProfile.kt（pi.profile  dormant 能力） | 无品牌暴露；删它是 build-logic 手术，收益零风险有 |

## 已删除/清除

- `.kimi-code/skills/`：**13 个 运行框架 通用技能全删**（azurpilot-cli-operate/azurpilot-diagnose/azurpilot-interface-guide/azurpilot-pipeline-×6/azurpilot-project-×2/azurpilot-wiki/azurpilot-workflow-build）；`azurpilot-azurpilot-phone-debug` 审阅后**整体删除**——内容是 m0 时代 运行框架 pipeline 调试闭环（pi/ 热推、azurpilot.log、on_error），与 v3 架构（wrapper 22400/桥 22300/runner 日志）完全不符，留着只会误导；v3 取证链以 devlog 2026-09-19 条目为准。
- `.kimi-code/mcp.json`：删 `azurpilot-mcp`、`create-azurpilot-project` 两个 MCP server（保留 playwright）。
- pi-profile 配方残留：`pi-profile.m0.yaml`（根）、`app/pi-profile.sample.yaml`、`app/INTEGRATION.md` 删除。
- 用户可见 azurpilot 文案修正：`run_log_empty`（"运行框架 的运行输出"→"AzurPilot 的运行输出"）、`settings_debug_mode_desc`、`dialog_enable_debug_message`（zh+en 共 6 条）；`provider_paths.xml`/`UiText.kt`/`AppSettings.kt` 注释同步；`app/build.gradle.kts` jniLibs 注释、convention 插件 ABI 注释更名。
- README：删 上游组织/运行框架 致谢条目（上游 fork 架构参考条目保留）。

## 账册与 git

- devlog 已入账（2026-09-20 更名条目两条）。
- **所有改动未 commit/push**（等用户指令）。涉及面：README/AGENTS/CHANGELOG 无冲突，app 资源与构建注释、rootfs+assets 双源 regen_args、.kimi-code、删除的文件。
- 发版（用新名出 `AzurPilot-v*` APK + Release）需用户另行授权。

## 标识符断代整改 + 真机复验（2026-09-20 晚，本篇最终状态）

- **整改**：`azurpilot`→`azurpilot` / `AzurPilot`→`AzurPilot` / `MAAAL_`→`ALASAOS_` 三轮 replace_all 完成；文件改名 `module/device/method/azurpilot.py`、`seeds/azurpilot_update.sh`；残留扫描零命中（含补抓的 rootfs/build 两处 R1 漏网——其中 spike-f-ocr-gate.py 设 `MAAAL_OCR_MODEL_DIR` 而 rpc.py 读 `ALASAOS_OCR_MODEL_DIR` 是功能断链，非仅命名）。双源 cmp 全一致；py AST / sh -n / assembleRelease 全绿。
- **真机复验事故**：新包首启 wrapper 3s 崩溃重拉 500+ 轮。根因=旧包 `com.aliothmoon.azurpilot`（v0.1.2）装机且会话存活占死三端口，`awaitServices` 被喂假 RUNNING。期间所有"旧码"证据均来自旧包 rootfs，新包栈健康。
- **两处加固**（已建入 APK 并验证）：
  - `AzurPilotOverlay.STALE_FILES`：每次启动删烘焙 tar 遗留旧名文件（azurpilot.py/update.sh），apply() 只加不删的兜底。
  - `ProotHost.supervise` 熔断：连续 5 次 <10s 秒退 → FAILED 停止重拉（防端口占位场景的无限崩溃循环）。
- **验证**：force-stop 双包止血 → 重装 → 干净 RUNNING；挂机冒烟全链路（azurpilot 桥值/游戏重启/登录成功/scheduler 排队）通过；零 azurpilot。
- **设备现状**：新包 `io.github.shinarin.azurpilot` 运行中（wrapper+gui 待机）；旧包 更早的旧名 已 force-stop 但仍装机——**建议用户择期卸载**（卸载清其私有区 AzurPilot 配置）。
- **git**：全部改动（更名三轮 + 两处加固 + 账册）未 commit/push，等用户授权；发版 v0.1.3（`AzurPilot-v0.1.3-android-arm64.apk`）亦需授权，notes 须带「卸载旧版→装新版→重授 Shizuku」。
