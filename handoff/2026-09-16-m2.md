# Handoff 2026-09-16 · 阶段二收官（M2-d DoD 6/6 真机验证通过）

> 新建对话或上下文压缩后：先读本文与 `docs/roadmap-v3.md`，再从「下一步」继续。
> 历史 handoff：`2026-09-15-repo.md`（建仓+阶段一开工点）、`2026-09-15-m1c.md`（M1-c 收官）等。

## 当前状态

- **阶段二（宿主外壳）全部完成并 push**：M2-a `b2fa54a`（fork 复活为 `app/` + 基线绿）→ M2-b `8bf608c`（减法 −19.6k 行）→ M2-c `05164f8`（特权进程内 Kotlin 桥，TCP 22300 五端点）→ M2-d `8c02157`（HostState 接管外壳状态，悬浮球/面板/FGS 脱钩 RunnerPort）+ 修复 `728dff6`（面板在屏时环境恢复不复活球）。远端 `github.com/Shinarin/AzurPilot` main 已同步。
- **M2-d DoD 真机验证 6/6**（HONOR PPG-AN00 / MagicOS 10 / shizuku-m adb 模式在线）：①Shizuku 权限链路全自动（NotRunning→NeedAuth→始终允许→bind→setup→VD→FGS→桥→球）；②VD flags 合规（无 SHOULD_SHOW_SYSTEM_DECORATIONS，手势三窗口全程 displayId=0）；③桥五端点 PC 客户端全对（SCREENCAP 恰 1280×720×3 BGR 裸帧）；④WebView 渲染真实 AzurPilot PyWebIO GUI；⑤悬浮球自动出现+FGS 在岗；⑥面板全生命周期（点球开面板→停止环境毁 VD→启动环境重建→X 关闭球复活）。
- **悬浮球消失之谜定案（非 bug）**：`android.settings.SETTINGS` 开在 VD 上时，MagicOS 把 `hideOverlayWindows` **全局**生效（主屏 overlay 被策略强隐，`mPolicyVisibility=false`）且 flag 粘性，需前台切换复评恢复。游戏无此属性（游戏前台球实测存活），生产无影响。详 `debug.md` 同日条目。**测试纪律：别把 Settings 类 app 起到 VD 上。**
- **清场完毕**：只剩 display 0、无 azurpilot 窗口/进程、手势窗口 displayId=0。
- 阶段一：M1-c 收官（GHA 第四跑绿，v4 artifact 交付基准）；**M1-d 油数 100 帧验收仍待用户把游戏点到出击菜单页**（游戏在验证期间被起到主屏后进程已退出，需用户重开并点到出击页）。

## 下一步（阶段三：管道穿透与生命周期）

按 roadmap-v3 第 5 节：首启解压 rootfs（进度条）→ 提权流水线接管（特权进程内拉起 proot/AzurPilot，替换 M1-d 的 shell 域 harness）→ proot FGS 保活 + 幻影进程自愈清锁 → Gitee 热更新通道 → wrapper（薄 runner 管进程组，Spike D 结论）→ 生命周期防护（VD 保活 userActivity 继承 m0、看门狗）。开工时先落 `app/` 内 rootfs 资产打包方案（v4 artifact 进 APK assets 还是首启下载——见 roadmap 决策）。

随时可穿插的待用户项：
1. **M1-d 油数验收**：用户把游戏重开并点到出击菜单页后，跑 `.tmp/m1d-oil-probe.py`（100 帧，设备 rootfs 已就位，WebUI harness 后台任务 bash-r6h84em7 保活中）。
2. **appId 身份**：`com.aliothmoon.azurpilot` → AzurPilot 命名（授权弹窗仍显示 "上游 fork"），改与不改、改成什么待用户定夺。
3. 阶段三清理候选：AppSettings 死 pref、`runner/DisplayResolution.kt` 包名不副实、toml 未用条目、rootfs 构建 `ALAS_REF` 浮动不钉（BUILD_MANIFEST 已记录解析后 commit）。

## 关键决策与注意

- **git 全预授权本次长任务期间有效**（commit/push 直接做；release/tag 仍需逐次授权）。
- 构建命令（Git Bash，判据=日志出现 BUILD SUCCESSFUL，勿信 exit code）：
  `cd app && JAVA_HOME='D:\VSCodeCache\shizku-m\build-env\jdk-17.0.2' GRADLE_USER_HOME='D:\VSCodeCache\azurpilot-azurpilot\.tmp\gradle-home' cmd //c 'gradlew.bat assembleDebug --console=plain'`
  APK → `app/app/build/outputs/apk/debug/app-debug.apk`（~73MB）。
- **真机操作纪律**（本次新立，debug.md 有对应条目）：tap 前必核旋转（`dumpsys input | grep orientation`）与目标当前帧坐标，禁止复用旧截图坐标——游戏上前台会把主屏转横屏，坐标空间随之换。
- 设备 WiFi adb `192.168.50.190:5555`（在线）；`export MSYS_NO_PATHCONV=1` 后才 adb shell；不私自锁屏/息屏；不代点游戏界面；VD 纪律见 `AGENTS.md`。
- 后台任务 bash-r6h84em7（proot WebUI 22267 保活）仍在跑，勿动。
- 排查悬浮窗"消失"口诀：`mViewVisibility=0x8`=app 自己藏的；`0x0`+`mPolicyVisibility=false`=系统策略藏（debug.md 2026-09-16 条目）。
